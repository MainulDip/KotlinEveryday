package flow

fun main() {
    /**
     * Difference between Flow (Sequential) vs NonFlow(NonSequential)
     * Sequential = run/return per item of a collection one after one
     * NonSequential = run/return full collection at once
     */
}